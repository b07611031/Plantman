import numpy as np
import matplotlib.pyplot as plt

# Example image matrix (assuming you already have it as a NumPy array)
# If you are using the image_matrix from your API response, make sure it's a NumPy array first
Image.fromarray(image_matrix)
image.show()